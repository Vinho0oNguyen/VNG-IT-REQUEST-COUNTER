# Optional. The hosting platform supplies PORT automatically.
PORT=

# Mount a persistent volume at /data in production.
REQUEST_COUNT_FILE=

# Used only when the persistent counter file does not exist yet.
REQUEST_COUNT_INITIAL=
