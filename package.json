{
  "name": "vng-it-request-counter",
  "version": "1.0.0",
  "private": true,
  "description": "A small web application that counts incoming requests.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
